<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:name=".AIGuardApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_shield"
        android:label="AI Guard"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIGuard">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ConsentActivity" android:theme="@style/Theme.AIGuard" />
        <activity android:name=".PasscodeSetupActivity" android:theme="@style/Theme.AIGuard" />
        <activity android:name=".PasscodeVerifyActivity" android:theme="@style/Theme.AIGuard" />
        <activity android:name=".ReviewSessionActivity" android:theme="@style/Theme.AIGuard" />
        <activity android:name=".ReportActivity" android:theme="@style/Theme.AIGuard" />
        <activity android:name=".SettingsActivity" android:theme="@style/Theme.AIGuard" />

        <service android:name=".service.MonitoringService" android:foregroundServiceType="mediaProjection|location" android:exported="false" />
        <service android:name=".service.OverlayService" android:exported="false" />
        <service android:name=".service.DailyCheckIn" android:exported="false" />

        <receiver android:name=".service.BootCompletedReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
